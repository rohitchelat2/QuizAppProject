import { test, expect } from '@playwright/test';
  /*
test('test', async ({ page }) => {
  await page.goto('http://localhost:5173/');
  expect(await page.title()).toHaveTitle("Learning app");
});



 await page.locator('div').filter({ hasText: 'Learning app for Spaced' }).nth(2).click();
  await page.getByText('Spaced repetition!').click();
  await page.getByRole('button', { name: 'Try it now!' }).click();
  await page.getByPlaceholder('Enter your email').click();
  await page.getByRole('paragraph').getByRole('button', { name: 'Register' }).click();
  await page.getByPlaceholder('Enter your email').click();
  await page.getByPlaceholder('Enter your email').fill('asd@asd.asd');
  await page.getByPlaceholder('Enter your email').press('Tab');
  await page.getByPlaceholder('Enter your password', { exact: true }).fill('asdasdasd');
  await page.getByPlaceholder('Enter your password', { exact: true }).press('Tab');
  await page.getByPlaceholder('Re-enter your password').fill('asdasdasd');
  await page.locator('input[type="submit"]').click();
  await page.getByPlaceholder('Enter your email').click();
  await page.getByPlaceholder('Enter your email').fill('asd@asd.asd');
  await page.getByPlaceholder('Enter your email').press('Tab');
  await page.getByPlaceholder('Enter your password').fill('asdasdasdd');
  await page.locator('input[type="submit"]').click();
  await page.locator('input[type="submit"]').click();
  await page.getByPlaceholder('Enter your email').click();
  await page.getByPlaceholder('Enter your password').click();
  await page.getByPlaceholder('Enter your password').fill('asdasdasd');
  await page.locator('input[type="submit"]').click();
  await page.getByRole('button', { name: '﹀' }).click();
  await page.getByRole('button', { name: '﹀' }).click();
  await page.getByRole('button', { name: 'Become admin' }).click();
  await page.locator('span').click();
  await page.getByPlaceholder('Add new topic').click();
  await page.getByPlaceholder('Add new topic').fill('New topic');
  await page.getByRole('button', { name: 'Add' }).click();
  await page.getByRole('button', { name: 'New topic' }).click();
  await page.getByRole('button', { name: '+' }).click();
  await page.getByLabel('Question').fill('Question 1');
  await page.getByLabel('Correct answer').click();
  await page.getByLabel('Correct answer').fill('1');
  await page.getByLabel('Option 1').click();
  await page.getByLabel('Option 1').fill('23');
  await page.getByLabel('Option 2').click();
  await page.getByLabel('Option 2').fill('34');
  await page.getByRole('button', { name: 'Add question' }).click();
  await page.getByRole('button', { name: '↺' }).click();
  await page.getByRole('button', { name: '12' }).click();
  await page.getByRole('button', { name: '12' }).click();
  await page.getByRole('button', { name: '12' }).click();
});*/